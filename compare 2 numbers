#include <stdio.h>

int comp(void) 
{
  int a,b,c;
  printf("Enter the 2 numbers : \n");
  scanf("%d",&a);
  scanf("%d",&b);
  if(a>b)
    printf("%d is bigger",a);
  else
    printf("%d is bigger",b);
}

int main()
{
  comp();
  return 0;
}
