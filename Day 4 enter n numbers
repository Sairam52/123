#include <stdio.h>

int main(void) 
{
  int n,num[10];
  printf("Enter the limit : ");
  scanf("%d",&n);
  for(int i=1;i<=n;i++)
  {
    printf("Enter the number : \n");
    scanf("%d",&num[i]);

  }
  return 0;
}
